"""PipeGraph application entry point.

Run with: python -m pipegraph
Or: pipegraph (if installed with entry point)
Or: uv run pipegraph
"""

from __future__ import annotations

from Qt.QtWidgets import QApplication


def main() -> int:
    pass
