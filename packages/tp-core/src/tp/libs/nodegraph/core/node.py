from __future__ import annotations

from abc import ABC


class BaseNode(ABC):
    """Base class for all nodes in the node graph."""


class Node(BaseNode):
    """Class that defines a node in the node grpah."""
