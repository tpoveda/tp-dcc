from tp.maya.managers import scene
from tp.libs.rig.freeform.core import menu


def startup():
	# menu.FreeformContextMenu()
	# scene.SceneManager().register_update_method(menu.FreeformContextMenu.refresh)
	pass

def shutdown():
	# scene.SceneManager().remove_method(menu.FreeformContextMenu.refresh)
	# menu.FreeformContextMenu().delete()
	pass